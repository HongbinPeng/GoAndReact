# interface_demo

这个目录用于练习 Go 里的 `interface` 关键字。

包含的知识点：

- 接口的定义
- 类型对接口的隐式实现
- 接口组合
- `any` / `interface{}` 的基本使用
- `type switch` 判断实际类型

运行方式：

```powershell
cd week04/practice/interface_demo
go run .
```
